# vim: set sts=4 expandtab:
IM_CONFIG_SHORT="$(gettext "activate the bare XIM with the X Keyboard Extension")"

IM_CONFIG_LONG="$(gettext "This forces not to use any external IM.
This activates the bare XIM with the X Keyboard Extension for all softwares.
This is the automatic configuration choice if no required IM packages are installed.")"

# This always return true
package_auto () {
:
}

# This always return true
package_menu () {
:
}
